# L19: Validate calendar, tournaments, qualification and rankings

State at audit: **partial**. Launch gate: **required**. Dependencies: L04, L15. Legacy audit packages: 17, 35, 38.

## Copyable master prompt

```text
Work in C:\Users\IsacC\EsportSimulator on the selected launch package. Read DEV.md, TASK.md, LEARNINGS.md, docs/launch-readiness/README.md and this package's current evidence. The target is a Windows 1.0 release, confirmed by the owner. Preserve fictional team names, the 2D radar, quiet Map Studio, existing careers and user map drafts. Preserve unrelated changes; do not reset the workspace.

Reverify current code before fixing an old finding. Reuse working systems and canonical serializers; do not rebuild completed features. Inspect dependency packages and complete independent preparation where possible, but do not mark this package accepted while a required dependency lacks evidence. Treat conditional packages as required whenever their feature is shipped or advertised.

Implement in reviewable increments, with meaningful regression tests for behavior changes. Keep engine logic deterministic and platform side effects outside it. Use real browser/native acceptance for UI and platform behavior; mocked tests and static scans are not substitutes. Do not weaken validation, expand audit allowlists or change promised scope just to obtain a green result. Use current primary documentation for dependency/platform changes and applicable skills when needed. Respect the session's authorization for external actions; prepare reviewable artifacts before any final approval that is actually required.

After implementation, record files changed, exact checks and results, build/fixture identity, screenshots or logs where relevant, limitations and remaining tasks in docs/launch-readiness/evidence/L19-REPORT.md. Update this package's status and evidence references in backlog.json and CHECKLIST.md, then update TASK.md. An unchecked acceptance criterion remains open. Do not claim perfection, Steam approval, rights clearance or a completed release without the required evidence.

SELECTED PACKAGE: L19 — Validate calendar, tournaments, qualification and rankings
ENTRY POINTS: engine/tournament; data/tournaments.json; app/schedule/page.tsx; app/tournaments; app/rankings/page.tsx
DEPENDENCIES: L04, L15

WORK REQUIRED:
1. Verify every declared format and supported field size, byes, tiebreakers, brackets, map series, qualification, seeding and terminal completion.
2. Use shared calendar helpers for days, weeks, seasons, contract dates and historical fixtures; resolve overlaps and rollover correctly.
3. Award trophies, prize money, circuit points and qualification exactly once, then propagate consistent results to schedule, inbox, rankings and history.
4. Ensure AI-only fixtures and unavailable player teams progress the world without softlocks; distinguish implemented formats from labels that fall back.

ACCEPT ONLY WHEN:
1. All launch tournament formats finish through actual scheduled progression with correct entrants and winners.
2. No team appears twice, receives duplicate byes/rewards or advances from an unresolved result.
3. Multi-season calendars and rankings remain coherent after save/reload, job changes and eliminated player teams.

DELIVER: the implementation and migration/rollback notes where relevant; regression and real-environment evidence; L19-REPORT.md with remaining limitations; updated backlog and TASK status. If a required external resource is unavailable, finish independent local work and identify the exact missing evidence without pretending the gate passed.
```

